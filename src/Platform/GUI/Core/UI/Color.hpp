#pragma once

#include "imgui.h"

namespace Core {
    
    namespace Color {

        constexpr ImU32 LightRed = IM_COL32(255, 160, 122, 255); // lightsalmon
        constexpr ImU32 LightOrange = IM_COL32(255, 209, 146, 255);
        constexpr ImU32 LightYellow = IM_COL32(255, 255, 153, 255); // lightyellow3
        constexpr ImU32 LightGreen = IM_COL32(144, 238, 144, 255); // lightgreen
        constexpr ImU32 LightBlue = IM_COL32(168, 197, 255, 255);
        constexpr ImU32 LightViolet = IM_COL32(229, 204, 255, 255);

        constexpr ImU32 Red = IM_COL32(220, 20, 60, 255); // crimson
        constexpr ImU32 Orange = IM_COL32(255, 165, 0, 255);
        constexpr ImU32 Yellow = IM_COL32(255, 255, 0, 255);
        constexpr ImU32 Green = IM_COL32(124, 252, 0, 255); // lawngreen
        constexpr ImU32 Blue = IM_COL32(53, 100, 255, 255);
        constexpr ImU32 Violet = IM_COL32(147, 112, 219, 255); // mediumpurple

        constexpr ImU32 DarkRed = IM_COL32(139, 0, 0, 255); // darkred
        constexpr ImU32 DarkOrange = IM_COL32(255, 128, 0, 255);
        constexpr ImU32 DarkYellow = IM_COL32(255, 218, 3, 255); //sunflower
        constexpr ImU32 DarkGreen = IM_COL32(0, 100, 0, 255); // darkgreen
        constexpr ImU32 DarkBlue = IM_COL32(0, 0, 128, 255); // navy
        constexpr ImU32 DarkViolet = IM_COL32(128, 0, 128, 255); // purple

        constexpr ImU32 White = IM_COL32(248, 248, 255, 255); // actually ghostwhite
        constexpr ImU32 Gray = IM_COL32(192, 192, 192, 255); // actually silver
        constexpr ImU32 Black = IM_COL32(0, 0, 0, 255);

        constexpr ImU32 Cyan = IM_COL32(64, 224, 208, 255); // actually turquoise
        constexpr ImU32 CaputMortuum = IM_COL32(89, 39, 32, 255);
        constexpr ImU32 BarbiePink = IM_COL32(255, 0, 127, 255);
        constexpr ImU32 Pink = IM_COL32(255, 51, 255, 255);

    };
}
